import React from 'react';

const Input = ({value}) => {
    return (
        <div>
            <input type="text" value={value} onChange={()=>{}} placeholder='calculate some 0' />
        </div>
    );
};

export default Input;